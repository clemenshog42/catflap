# Raspberry Pi specific dependencies
# Note: 'picamera2' is intentionally excluded. Install it via apt:
# sudo apt install python3-picamera2

numpy
opencv-python-headless
ultralytics
flask
ai-edge-litert
gpiozero
flasgger
